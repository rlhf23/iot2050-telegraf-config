name: Build Control Service Binary

on:
  workflow_dispatch:
  push:
    branches: [main, master]
    paths:
      - 'docker/control-service/**'

jobs:
  build:
    name: Build for ${{ matrix.arch }}
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        include:
          - target: aarch64-unknown-linux-musl
            arch: arm64
            needs_cross: true
          - target: x86_64-unknown-linux-musl
            arch: amd64
            needs_cross: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross tool
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-${{ matrix.arch }}-control-cargo-registry-${{ hashFiles('docker/control-service/Cargo.lock') }}

      - name: Cache cargo index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-${{ matrix.arch }}-control-cargo-git-${{ hashFiles('docker/control-service/Cargo.lock') }}

      - name: Cache target directory
        uses: actions/cache@v4
        with:
          path: docker/control-service/target
          key: ${{ runner.os }}-${{ matrix.target }}-control-target-${{ hashFiles('docker/control-service/Cargo.lock') }}

      - name: Build control-service with cross
        run: |
          cd docker/control-service
          cross build --release --target ${{ matrix.target }}

      - name: Verify binary
        run: |
          ls -lh docker/control-service/target/${{ matrix.target }}/release/control-service

      - name: Prepare binary for upload
        run: |
          mkdir -p artifacts
          cp docker/control-service/target/${{ matrix.target }}/release/control-service \
             artifacts/docker-control-service-${{ matrix.arch }}
          chmod +x artifacts/docker-control-service-${{ matrix.arch }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: control-service-${{ matrix.arch }}
          path: artifacts/docker-control-service-${{ matrix.arch }}
          retention-days: 90

  commit-binaries:
    name: Commit updated binaries
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download arm64 artifact
        uses: actions/download-artifact@v4
        with:
          name: control-service-arm64
          path: artifacts/arm64

      - name: Download amd64 artifact
        uses: actions/download-artifact@v4
        with:
          name: control-service-amd64
          path: artifacts/amd64

      - name: Update binaries in repo
        run: |
          cp artifacts/arm64/docker-control-service-arm64 docker/control-service/docker-control-service-arm64
          cp artifacts/amd64/docker-control-service-amd64 docker/control-service/docker-control-service-amd64
          chmod +x docker/control-service/docker-control-service-arm64
          chmod +x docker/control-service/docker-control-service-amd64

      - name: Commit and push
        run: |
          git config user.email "action@github.com"
          git config user.name "GitHub Action"
          git add docker/control-service/docker-control-service-arm64 docker/control-service/docker-control-service-amd64
          git diff --quiet && git diff --staged --quiet || git commit -m "chore: Update pre-built control-service binaries"
          git push

      - name: Create Release (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/arm64/docker-control-service-arm64
            artifacts/amd64/docker-control-service-amd64
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}